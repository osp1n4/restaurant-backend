import { Router } from 'express';

const router = Router();

// NOTA: Las rutas de analytics están en analyticsRoutes.ts
// El router admin actualmente no tiene rutas propias
// pero está listo para agregar rutas administrativas en el futuro

export default router;
